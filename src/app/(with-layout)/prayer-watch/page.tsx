import PrayerContainer from "@/components/(withLayout)/Prayer-Watch/PrayerContainer";

const PrayerWatchPage = () => {
    return (
        <div>
          <PrayerContainer></PrayerContainer>
        </div>
    );
};

export default PrayerWatchPage;